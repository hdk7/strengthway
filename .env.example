VITE_API_BASE_URL=http://localhost:5000/api

# Demo mode lets the admin login form work without a backend, using a hardcoded
# credential pair. Set to false (or remove) once VITE_API_BASE_URL is live.
VITE_DEMO_MODE=true
